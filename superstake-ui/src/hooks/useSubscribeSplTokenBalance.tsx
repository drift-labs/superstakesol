// import { useEffect, useId, useRef } from "react";

// /**
//  * Listen to the SPL balance of a set of mints (using their mint address and ATA only)
//  * @param tokenMintAddress
//  * @returns
//  */
// const useSubscribeSplTokenBalance = (
// ) => {
//   const splSubscriptionMap =
//   const hookId = useRef(`${Math.random()}`.slice(1));

//   useEffect(() => {
//     return () => {
//       // Remove this hook's spl subscription from context mapping
//     };
//   }, []);
// };

// export default useSubscribeSplTokenBalance;
